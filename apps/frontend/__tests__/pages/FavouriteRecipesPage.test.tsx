import '@testing-library/jest-dom';
import { TestContext } from '../utils';
import FavouriteRecipesPage from '@fe/pages/favourite';

const testContext = new TestContext();

describe(FavouriteRecipesPage.name, () => {
  it.todo('renders favourite recipes');
});
