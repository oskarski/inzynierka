import { AppPopup } from '@fe/components';

export const SearchRecipesByIngredientsPopupContent = () => {
  return (
    <AppPopup.Content>
      <AppPopup.Title>Szukaj przepisów</AppPopup.Title>
    </AppPopup.Content>
  );
};
